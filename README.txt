# お金箱 v2.2 更新ファイル

既存の `money` リポジトリに以下3ファイルを上書きしてください。

- index.html
- manifest.webmanifest
- sw.js

## v2.2
- 「キャンセル」を「戻る」に変更
- 戻るボタンを押しても閉じない問題を修正
- 右上の × も確実に閉じるよう修正
- ダイアログ外側をタップしても閉じられるよう改善
- 複雑だったブラウザ履歴連携を外し、操作画面の開閉を安定化
- v2.1の「給料振り分け画面で割合を直接変更」機能はそのまま維持
- キャッシュをv4へ更新

既存データは同じ `moneybox-v1` 保存領域を使うため、残高・履歴は維持されます。
